Build the Orchard binary for the current platform:

```bash
go build -o orchard ./cmd/orchard && echo "Built: ./orchard"
```

To run the binary, type `! ./orchard` in the prompt to execute it directly in the terminal.
